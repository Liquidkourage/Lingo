# Legacy Firebase Prototype

This folder is **not** the current online path.

It contains the earlier static browser pages that were built around the Firebase prototype:

- `index.html`
- `host.html`
- `bingo.html`

These are kept for reference while the active online migration moves to:

- `../online/` for the Railway + Postgres app

Use `online/` for current deployment work.
