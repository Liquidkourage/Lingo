# Legacy Firebase Functions Prototype

This folder contains the earlier Firebase Functions backend prototype.

It is kept for reference only. The current online migration path is:

- `../online/` for the Railway + Postgres app

Only use this folder if you intentionally want to revisit the old Firebase approach.
